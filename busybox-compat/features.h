/* features.h — mlibc compatibility stub for Busybox cross-compilation.
 * mlibc does not install features.h; this stub satisfies the include.
 */
#ifndef _FEATURES_H
#define _FEATURES_H

/* Nothing needed — mlibc sets its own feature-test macros internally. */

#endif /* _FEATURES_H */
